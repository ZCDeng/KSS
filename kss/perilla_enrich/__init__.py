"""紫苏叶个股富化层.

给紫苏叶列表(注册表 ``core`` + ``main`` 两层)的个股叠加只读富化数据:

1. **机构持仓动态** — Tushare 前十大流通股东季度环比 + 北向(陆股通,经香港中央结算行)趋势.
2. **PE 动态** — PE_TTM 现值 + 历史分位.
3. **美股对标估值** — 注册表 ``us_peer`` 字段 + yFinance 取对标 PE/市值/价格, 算估值差.

设计约束:
- 所有外部取数带本地缓存(``storage/perilla_cache`` / ``storage/us_peer_cache``).
- 每项独立优雅降级(网络墙/无权限/对标未标注 → status=unavailable/no_peer, 不抛、不阻塞其他项).
- 金融数字全代码确定性计算, 无 LLM 介入.
"""
