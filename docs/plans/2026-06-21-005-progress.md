# 进度：KSSDeck 独立化打包 (plan 005) — ✅ 全部完成

分支 `feat/kssdeck-standalone-packaging`。计划 `docs/plans/2026-06-21-005-feat-kssdeck-standalone-packaging-plan.md`。
11 个实现单元全部落地 + 验证 + 提交 + 推送。

## 提交一览
| 单元 | commit | 内容 |
|---|---|---|
| U0 | `93a3ca4` | pyproject.toml + uv.lock 捕获 .venv-desktop 闭包(Python 3.11) |
| U1 | `5c5a779` | 双模式经 KSS_PROJECT_ROOT 判定 + 可变写入引到 KSS_STATE_ROOT(paths/bridge/9 子脚本/Swift resolveRoots+breadcrumb) |
| U4 | `8467f9c` | 版本化桥协议 {schemaVersion,data} + Swift 两段解码 + schemaMismatch 横幅 |
| U2 | `e1c337d` | 首启 uv bootstrap 到 state-root venv(取代 vendor)；resolvePython/provisionRuntimeIfNeeded |
| U8 | `cbf92aa` | .app Developer ID 最小签名脚本(无逐 dylib，无 disable-library-validation) |
| U3 | `9f57dba` | 网络凭据 Keychain + network.env + Settings 面板 + 注入优先 |
| U5 | `b2039a3` | 常驻 sidecar(Unix socket)+ dispatch 抽取 + Swift socket 客户端 + subprocess 回退 |
| U6a+b | `cb3ab66` | kss-mcp(11 读 tool + paper-only 写闸)+ fastmcp 入 lock |
| U9 | `35ba622` | 三层脚本解析(KSS_SCRIPTS_ROOT)+ 双版本号 + 非阻塞 lock 刷新 + SIGHUP 重载 |
| U7 | `a0ca088` | commentary 百分比幻觉结构后盾(_neutralize_fabricated_percentages) |

## 验证
- swift build OK；全 Python 编译 OK；bridge snapshot schemaVersion=1 stockCount=165。
- dev/bundle 双模式 snapshot 读路径均通过(state-root sim)。
- sidecar 第二次调用 5ms(vs subprocess ~300ms+)；实机 app spawn daemon → socket 0700 → UI 完整加载经 sidecar。
- U2 bootstrap：UV_PROJECT_ENVIRONMENT uv sync 出可用 venv，bundle-mode bridge 跑通。
- U6a kss-mcp：paper-only 11 tools、live 13 tools、dispatch 返 165。
- U9 KSS_SCRIPTS_ROOT override 端到端 + override VERSION=0.2.0-synced 读到。
- U7：73 commentary 测试全过(含新增 guard 测试)。
- 最终集成冒烟：app + sidecar + socket + PID 文件全就位。

## 有意偏离 plan（已记录于各 commit）
- U4 leaderStocks 双 decoder **保留作向后兼容 shim**(不删不迁移，更稳)。
- U6 standalone stdio MCP(非 co-host sidecar)；现有 datasette/plugins/kss_mcp 是废弃 pilot cruft(建议清理)。
- U7 用 server-side 百分比中和(非 Pydantic value:null JSON 重构 + <kss-number> 新槽)；(b)(c) deferred。
- U2/U0 Python 锁 3.11(匹配现状，非 plan 写的 3.12)。

## 待用户机器执行(需 full Xcode + Developer ID)
- `script/sign_and_build.sh`：实际 codesign + 公证(commented slot)。
- `swift test`：BridgeEnvelopeTests + 其他套件(CLT 无 XCTest)。

## 已知 bundle-mode 待真机核验项(非阻塞)
- update_cs_data 写 cs_data(已重定向，需真 bundle 跑确认)。
- LaunchAgent 兜底生命周期(app-spawn 已工作)；stale-socket 由 subprocess 回退兜底。
- MCP 强校验(app 一次性 token)；commentary (b)sqlite 追问 /(c)决策账本。
