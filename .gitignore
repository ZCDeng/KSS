# Secrets / env
.env
.env.local
.env.*.local
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtualenv
.venv/
.venv-*/
venv/
env/

# IDE / OS
.DS_Store
.idea/
.vscode/

# Claude / agents 私有状态
.claude/

# 数据（由 scripts/update_cs_data.py 重建，体积大）
storage/*.db
storage/*.db-journal
storage/*.db-wal

# 审计底稿例外：storage/daily_review/ 与 storage/etf_radar/ 是预测校验
# (validate_predictions.py) 的依据，纳入版本控制；周五校验 cron 自动 commit

# 日志 / 临时
*.log
/tmp/*

# Jupyter
.ipynb_checkpoints/

# 临时回测产物（保留 storage/reports/ 的 *.md，不保留 png 中间结果）
*.parquet

# Kronos 离线产物：冻结模型 artifact + 预测 sqlite（可由批处理重建，不入库）
storage/kronos/

# 个股资金流 / 指数行情数据（体积大，由数据脚本重建，不入库）
mf_*.csv
idx_*.csv

# agent / 工具私有状态（同 .claude/）
.pi/

# storage 缓存 / legacy（可重建，不入库）
storage/bj_cache/
storage/legacy/

# scan 排名输出（数据产物，可重建）
scan_*_rank.csv

# SwiftPM 桌面 app 构建产物
/dist/
storage/trends/

# 回测闭环运行时 DB(可重建，不入库)
storage/prediction_ledger/
storage/factor_health/
storage/cpcv_cache/
storage/pipeline_alpha/
